<%= form_for(@businessstatus) do |f| %>

  <%= render "errormsg" , modeldata: @businessstatus %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
