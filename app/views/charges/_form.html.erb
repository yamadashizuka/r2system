<%= form_for(@charge) do |f| %>
  <% if @charge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@charge.errors.count, "error") %> prohibited this charge from being saved:</h2>

      <ul>
      <% @charge.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :charge_date %><br>
    <%= f.date_select :charge_date %>
  </div>

  <div class="field">
    <%= f.label :charge_comment %><br>
    <%= f.text_area :charge_comment %>
  </div>

  <div class="actions">
    <%= f.submit  t('views.buttun_charge')%>
  </div>
<% end %>
