<%= form_for(@paymentstatus) do |f| %>
  <% if @paymentstatus.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@paymentstatus.errors.count, "error") %> prohibited this paymentstatus from being saved:</h2>

      <ul>
      <% @paymentstatus.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
